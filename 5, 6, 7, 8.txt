5. Java Script Advantages ?

	Javascript is a dynamic computer programming language.  
	It is lightweight  most commonly used as a part of the web pages, Whole 	implementation a client-side script to interact with a user and to make dynamic pages.  
	It is programing language with object -orirented.

6.  For in and for each difference

	i.	For in satement loops through the properties of an object.
	
		for (key in object)
		{
			statements
		}

		
	ii. 	The forEach() method calls a function for each element in an array.

		The forEach() method is not executed for empty elements.

		const fruits = ["apple", "orange", "cherry"];
		
		fruits.forEach(myFunction);

7. What is closure?

	JavaScript variables can belong to the local or global scope.

	Global variables can be made local (private) with closures.

	function myFunction()
	 {
  		let a = 4;
  		document.getElementById("demo").innerHTML = a * a;
	}
	
8. Write 5 math inbuilt method.

	i.	 Math. round() 
	ii.	 Math. pow() 
	iii.	 Math. sqrt() 
	iv.	 Math. abs() 
	v.	 Math.ceil() 